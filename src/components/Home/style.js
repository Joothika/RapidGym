import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    h1: {
      fontFamily: "Inter",
    },
    h2: {
      fontFamily: "Teko",
    },
    h3: {
      fontFamily: "Anton",
    },
  },
});

export default theme;
